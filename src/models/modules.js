// Módulos do Fiscal Fácil
const MODULES = {
  DEFAULT: 'default',
  ROOT: 'root',
  PARTNER: 'partner',
}

// Array com todos os módulos
const allModules = Object.values(MODULES)

export default MODULES

export { MODULES, allModules }
